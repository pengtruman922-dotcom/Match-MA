# 给「行业字典下线 + 标的行业字段改造」调研设计的开场白（2026-09-08）

> 复制以下全文，在新对话里发给负责设计的 AI。

---

你好。我要去掉「设置 - 数据字典 - 行业字典」这一整套东西，并把**标的侧的行业字段**改成
和**买家侧**一致的口径；买家信息（buyer_party）解析这条链也要同步调整。

**这一轮只做调研和设计，不要改任何代码、不要建迁移、不要动 prompt。**
我要的产出是一份可以直接当施工单用的方案文档。

## 背景：这是买家侧走过一遍的路，标的侧还没走

- **2026-08-24 / 020 迁移**给买家主体做过一次：`industries_json` + `industry_l2_json`
  → `business_tags_json`（自由标签）。迁移注释里写了为什么行业字典接不住买家的细分主业。
- **2026-08-28 判决一**把买家需求侧的行业字典也下线了，改走「自由标签 + 自由文本」
  （`business_tags_json` + `scenario_summary` / `other_requirements_text`）。
  方案见 `平台优化方案/买家需求字段精简与反向检索方案0828.md`。
- **标的侧至今没动**，还停在 `industry_pairs_json`（受控字典配对）+ `industry_l1` /
  `industry_l2`（兼容投影列）上。于是两边口径不一致：买家说的是自由词，标的存的是字典项。

指标注册表 `backend/app/registry/indicators.py` 第 300 行附近那段注释正好说的是这件事——
判决一解散的是「买家行业词 ↔ 标的 `industry_pairs_json`」这个跨侧契约本身，
但标的侧的 screening 角色当时**被保留了下来**。这一轮就是来收这个尾的。

## 先读这些

按顺序：

1. **`AGENTS.md`** —— 项目硬规则（部署拓扑、Git、测试验证、迁移、同步约束）。**优先级最高。**
2. **`docs/系统总纲.md`** —— 系统权威描述。重点看 §2.2（两层信息结构）、
   **§2.3（数据字典原则）**、§5（指标注册表）。这一轮很可能要改写 §2.3。
3. **`平台优化方案/买家需求字段精简与反向检索方案0828.md`** —— 判决一的完整论证。
   本轮标的侧的取舍要么照抄它，要么明确说明为什么标的侧不一样。
4. **`database/migrations/020_buyer_party_facts.sql`** —— 买家主体那次合并的样板，注释即理由。
5. **`backend/app/registry/indicators.py`** —— 字段闭集、中文名、可写来源、初筛角色的唯一真源。

## 爆炸半径：我已经量过了，直接从这里开始查

**后端引用 `industry_taxonomy` 的地方（12 处，含服务层与 handler）：**

| 文件 | 用法 |
| --- | --- |
| `backend/app/services/industry_taxonomy.py` | 字典服务本体（`resolve_l1` / `normalize_industry_pairs` / `normalize_l2_values` / `list_l1_terms` / `industry_l1_prompt_list` / `industry_l2_prompt_list`） |
| `backend/app/api/routes/data_dictionaries.py` | 字典 CRUD + 导入（配 `services/industry_dictionary_import.py`） |
| `backend/app/api/routes/model_config.py` | **把字典闭集注入 prompt 预览** |
| `backend/app/jobs/handlers/model_node_test.py` | 同上，节点自测时注入 |
| `backend/app/jobs/handlers/seller_target_parse.py` | 标的解析：把字典塞进 prompt，回来再归一 |
| `backend/app/jobs/handlers/buyer_intent_parse.py` | 买家需求解析：同上（判决一之后残留多少要查） |
| `backend/app/jobs/handlers/business_update.py` | 业务动态抽取链路 |
| `backend/app/jobs/handlers/research_map.py` | 调研结果回填时用 `list_l1_terms` 归一 |
| `backend/app/services/field_writer.py` | 统一写入口，行业列写入前强制归一 |
| `backend/app/services/research_apply.py` | 调研提案落库同上 |
| `backend/app/services/recommendation_conditions.py` | 推荐条件解析 |
| `backend/app/api/routes/seller_targets.py` | `_normalized_create_industry_pairs()`（第 441 行），新建标的时归一 |
| `backend/app/api/routes/meta.py` | 元数据接口暴露字典词条与计数 |

**硬筛与外部 Agent：**

- `backend/app/services/screening_schema.py` —— 文件头注释明写「行业闭集在运行时从
  `industry_taxonomy` 注入」；第 76 行的 `value_type` 里有
  `industry_l1` / `industry_l2` / `industry_any` 三种取值类型。
- `backend/app/services/screening_sql.py:453` `_industry_text()` —— 把
  `industry_pairs_json` 渲染成给模型看的文本。
- `backend/app/services/agent_target_views.py:126,276` —— MCP 返回给外部 Agent 的「行业」字段。
- `backend/app/mcp/tools.py` **本身没有任何 industry 字样**——行业条件是通过
  `screening_schema.build_conditions_properties()` 自动进 `targets_filter` 的 schema 的。
  这意味着**改注册表就会改 MCP 的对外契约**，这是本轮最需要想清楚的一处。

**前端：**
`features/settings/IndustryDictionary.tsx`（设置页本体）、`features/settings/PromptDrawer.tsx`、
`components/CascadeFilter.tsx`、`features/buyers/presentation.tsx`、`pages/Dashboard.tsx`、
`types/api.ts`。

**注册表现状（`indicators.py`）：**
标的侧 `industry_pairs_json`（第 169 行，`writable_by=_BOTH_MANUAL`）；
`industry_l1` / `industry_l2` 在第 654 行起被标为 `industry_pairs_json` 的兼容投影（总纲 §2.3）；
买家侧 `industries_json` / `industry_l2_json` / `excluded_industries_json` /
`industry_focus_tags_json` / `industry_primary` / `industry_secondary` 已全部标「已退役」；
对照面是买家主体的 `business_tags_json`（第 494 行）与买家方案的 `business_tags_json`（第 554 行）。

## 需要你回答的问题

1. **行业字典现在到底谁在用**：写入方、SQL 硬筛、prompt 注入、前端展示、MCP schema
   五条线各是什么状态？拆掉之后哪些地方会塌、哪些只是少一个可选条件？
2. **标的行业改成自由标签 + 文本之后，硬筛怎么办**：`targets_filter` 的行业条件是彻底去掉、
   换成标签包含匹配、还是保留 `industry_l1` 作兼容列？
   **注意我的首要目标是外部 Agent 检索的完整性和准确率**——正反向匹配都在 wegent 上的 Agent 做，
   Match-MA 只做信息真源 + 只读查询。所以判断标准是「Agent 拿到这个字段能不能更准地筛」，
   不是「后端 SQL 能不能少写一个 join」。
3. **`industry_l1` / `industry_l2` 的归宿**：退役成死列（保留数据不再写）、还是真删列？
   删列的两阶段方案怎么排？
4. **买家信息（`buyer_party`）解析要同步改什么**：哪些 prompt 的行业契约要改、
   归一逻辑要去掉哪几处、`business_tags_json` 的标签粒度和数量约定是什么？
5. **存量数据怎么迁**：阿里云自建现在有 **93 个标的 / 24 个买家**（Railway 另有一份）。
   已有的 `industry_pairs_json` 值要不要回填成标签？回填规则是什么？谁来跑？
6. **分几步落地**，每步改哪些文件、怎么验证、失败了怎么回退。

## 两条硬约束

- **prompt 改版走设置页 /「Prompt 版本管理」/ `POST /model-config/prompts`，
  绝对不要写 prompt seed 迁移**（`AGENTS.md` 同步约束里写死了）。
- **两套部署的模型 / 节点 / prompt 配置是分别维护的**（Railway 是推送式、阿里云自建是拉取式），
  库里的配置不会随 `git pull` 或迁移过去。方案里必须有一节说明两边怎么同步、谁先谁后。

## 交付物

一份 `平台优化方案/行业字典下线与标的行业字段改造方案0908.md`，包含：
判决与理由 → 字段设计（改前 / 改后对照表）→ 爆炸半径清单（逐文件）→ 迁移与回填 →
接口契约变更（尤其 MCP `targets_filter`）→ prompt 改动清单 → 分步施工顺序 →
每步的验收方式 → 两套部署的同步说明 → 已知的坑。

**写完先给我看，我确认后再开新对话施工。这一轮不要动代码。**
