# 发给正在跑 4D 的对话（2026-08-18 追加）

> 4D 已经开工，所以这份不是新开场白，是**追加给它的一段话**。复制以下全文发过去。

---

补充：我在生产实测后提了五项反馈，已经写进总施工单
`平台优化方案/推荐升级阶段四施工单_Agent编排与追问芯片0818.md` 的新增小节 **§4D.5**。
请先读那一节，它与原有的 4D.1–4D.4 同等必做，**纳入本包，不另开施工包**。

摘要（细节以 §4D.5 为准，那里有已核实的现状与定位路径）：

1. **分节点计时，完成后与恢复历史时都要保留。**
   现在整轮只有一个前端秒表，流式一开始就停摆，恢复历史时归零后直接不显示。
   耗时要从落库消息读，不从前端计时器读——前端计时器活不过刷新和换页签。
2. **追问芯片要写成「用户下一句要说的话」，不是「给用户的建议」。**
   契约是对的（点击即发送），错的是语态：实测出现 `明确是否要求控股…`。
   §4D.5 给了五对正反例，直接抄进 Prompt。
   **主 Agent Prompt 因此从 v0.2.0 改发 v0.2.1**，4D.1 的版本表我已经同步。
3. **会话列表在正文吐完之前必须是黄点。**
   `running_sessions` 按 background job 判定，而 Writer 是 SSE 不占 job，
   所以 agent job 一结束就变绿点、正文还在吐字。判据要覆盖 Writer 阶段。
4. **正文里的标的现在全部不可点击**，是 4C 引入的回归。
   §4D.5 列了三个可能原因和各自的判据，**先从落库正文与同轮 brief 取真实样本定位，
   不要猜着改**。其中 `sanitize_writer_output` 里 `/targets/\S+` 的 `\S+`
   在中文正文中不遇空格会吃掉一整段——无论是不是本次现象的原因，这条都该修。

另外两点提示，不构成新要求：

- 这五项**不改变**任何已拍板口径（5 轮、40 家、3–6 家、required 单向放宽、
  去重只在汇总、追问只在芯片）。唯一的版本变化是主 Agent v0.2.0 → v0.2.1。
- 4D.2 的 UAT 用例 6（条件过严导致少量召回）请额外留意一件事：
  2.3 把「候选过少到什么程度才允许放宽 required」完全交给了主 Agent 判断，
  没有量化门槛。这是有意为之，但**放宽是否真的基于 `excluded_by_condition` 的
  marginal 拆分、而不是模型自己觉得少了**，只能靠这一组 UAT 看出来。
  请把该轮的 `agent_step` 放宽理由与上一次真实召回数一起贴进施工记录。
