# 重构施工单 0722

图纸：`docs/系统总纲.md`（v1.0，2026-07-22 定稿）。本单是其执行计划，做完把结论合并回总纲后归档。

批次顺序：**R1 基线压平 → R2 进度管理 → R3 指标注册表**。R1 已获批准；R2 规则已定稿；R3 设计已定稿（总纲 §5）。

---

## R1 基线压平（总纲 §6.1/6.2/6.3）

死对象清单以总纲 §6.1/6.2 为准：7 表（seller_target_financial、buyer_intent_constraint、seller_target_risk、seller_target_tag、tag_dictionary、seller_party、region_alias_config）+ seller_target 13 列 + buyer_intent 3 列 + 除 attachment 外的 visibility 列。

顺序（每步独立可发版，前一步部署验证后再进下一步）：

1. **R1-1 代码摘除 + 盘点端点**：删除对死对象的全部代码引用（seller_target_risk 两处空读、research.py 的 seller_party JOIN、meta.py 健康检查里的 tag/region 计数、INSERT 列表里的死列）；加临时 admin-only `GET /debug/table-stats`（全表行数）。无 schema 变更。
2. **R1-2 生产盘点**：用 table-stats 确认死表行数（审计只覆盖当前代码，历史版本可能写过数据）。死表有非垃圾数据则回到总纲重议，无则继续。
3. **R1-3 迁移 048**：drop 死表死列。走正常迁移流程（跑 `test_migration_sql.py`，部署盯 preDeploy）。
4. **R1-4 压平**：以 048 后的生产 schema 为准生成 `001_baseline.sql`（含必要种子：默认 team/workspace/admin、industry_taxonomy、model/node 配置骨架、prompt 当前生效版本从生产 dump 转正）；删除旧迁移文件与对应 alembic 版本脚本；新树单版本；生产 `alembic_version` 校准（stamp）。验收：全新库从 baseline 建出的 schema 与生产 `pg_dump --schema-only` diff 为零；CI 全绿。

风险与回滚：R1-3 前对生产做一次全量 dump 存档；压平只动迁移文件与版本表，不动数据。

## R2 撮合进度管理（总纲 §3.4，规则已定稿）——2026-07-22 完成并真机验证（`cfc7de8`）

- **R2a 详情页「推进」tab** ✅：标的/买家意向两侧镜像列表（状态、最新动态、下一步、无动态预警）+ 关系时间线抽屉（事件流、状态变更、手动记录动态）。`relations.py` 补 `PATCH /relations/{id}`（状态流转，进 buyer_seller_relation 更新记录可回滚）+ `POST /relations/{id}/events`（手动动态）+ `GET /relations-meta`；逻辑在 `services/relation_flow.py`。
- **R2b 推荐接线** ✅：候选带关系状态（`recommendation_flow._annotate_candidate_relations`）；卡片「已在推进·状态」+「查看推进」/「开始推进」/「正与其他买家深入推进」（尽调及以后，不透露对方）。
- **R2c 全局看板** ✅：`pages/Board.tsx`，五状态列 + 终态归档；后端 `/relations` 已按负责人可见性过滤，管理员看全局、顾问看自己。
- **R2d 对手方关联** ✅（**改为显式关联，非 LLM 解析路由**）：`POST /relations`（去重、开局 recommended + 开局事件）+ 推进 tab「关联对手方」搜索选择器。**deferred**：从自由文本跟进里 LLM 自动识别对手方并挂关系——需实体解析，留待后续；当前两条建关系路径（推荐「开始推进」、手动「关联对手方」）已确定性覆盖。

## R3 指标注册表（总纲 §5）

- **R3a** ✅（2026-07-23，真机验证）：`registry/indicators.py`（seller_target 逐列，唯一事实源）+ `GET /meta/indicators` + `TargetInfoPanel` 改为 API 驱动渲染（`infoGroups.ts` 降级为纯值格式化器）。parse/research 两个白名单已改为从注册表派生（`writable_columns`/`writable_enum_values`）。`test_info_group_sync` 删除，`test_indicator_registry` 用「注册表 screening ⊇ 打分器读取」+「注册表枚举 ⊆ DB check 约束」守护。**五消费方已收敛四个**（注册表 + parse + research + 前端渲染），打分器只读、由测试守护。
- **R3b**（待做）：唯一字段写入服务落地（手动编辑/解析采纳/调研采纳三来源收敛到 `写入(实体,字段,值,来源,证据)`，用注册表 kind/enum 校验归一化）；buyer_intent 接入注册表（再补 ~40 指标）；`field_value_source` 死表判决兑现。

## 随缘小项

- OCR key 并入 `model_provider_config` 加密 key 体系。
- 临时 `/debug/table-stats` 在 R1 结束后移除或留作运维端点（届时定）。

## 流程要求（每批次适用）

- 前端改动必须本地 dev server（连生产 API）浏览器过一遍再交付，不再只跑 typecheck/build/lint。
- 后端/迁移提交后轮询 `/health` 确认 hash 切换，再做业务验证。
- prompt 一律走设置页版本管理，不进迁移。
