# Match-MA 系统总纲

> **定位**：本文件是系统的图纸——回答「系统是什么、应该长什么样」。
> 施工单（`平台优化方案/*.md`）回答「接下来改什么」，从图纸出发，做完把结论合并回图纸。
> 图纸不带日期后缀、不另起新文件：就地修改，git 是版本历史。
>
> 最后更新：2026-08-18　|　当前状态：v1.0——正向智能推荐阶段四已完成 Agent 化收口（完整需求快照、多批独立初筛与公平候选池、单次深评回灌、brief v2、Writer 流式正文与独立追问芯片）；反向推荐与旧链路拆除待阶段五。

---

## 1. 业务与用户

**系统名称**：投资撮合管理和智能推荐平台（Match-MA）。

**业务背景**：咨询公司的投资撮合业务。顾问同时服务买卖双方——为买家寻找标的，为卖家寻找买家，并推进双方持续沟通。

**平台三大核心**：
1. 买家和卖家的信息管理与画像；
2. 智能推荐（双向）；
3. 撮合项目进度管理。

**用户与权限模型**（已实现，不需改动）：
- **顾问（consultant）**：录入信息、查看并管理自己负责的买家/卖家、发起推荐、记录进度。数据可见性 = 负责人本人 + 管理员（`owner_scope`）。
- **管理员（admin）**：全局明细可见、账号管理、系统设置（模型/prompt/搜索工具等 tab 仅管理员可见）。
- 账号管理的「最近活跃」只取业务更新、未撤回的关系动态、人工字段应用和推荐提问；AI 解析/调研等机器写入使用系统账号，不以业务对象 `updated_at` 冒充负责人活跃。
- 登录：`app_user` 账号密码 → JWT；另保留共享管理员凭证作恢复通道（`auth.py`）。
- **未启用**：team/workspace 多租户（列存在、值恒为种子常量）、`visibility` 三档字段（无过滤行为）。见 §6 判决。

## 2. 领域模型

### 2.1 核心实体

```
seller_target（标的，宽表 ≈80 列）──┐
buyer_party（买家主体）             ├── buyer_seller_relation（意向×标的 = 撮合关系）
buyer_intent（买家意向，宽表 ≈53 列）┘         └── relation_event（关系时间线）
        │
        ├── entity_profile_section（五组“其他”补充栏，多修订版本）
        ├── business_update → extracted_action → action_application_log（解析与更新审计链）
        ├── attachment → parsed_document → evidence_span（附件/OCR/证据）
        └── research_proposal（调研产出）
AI 基础设施：model_provider_config / model_node_config / prompt_template / background_job / ai_trace
数据字典：industry_taxonomy（L1/L2）
```

### 2.2 两层信息结构（定型，不再争论）

- **结构化字段**（宽表列）：服务 SQL 初筛与代码打分。宽表是「初筛必须代码化」的直接代价，通常**不换成 jsonb/纵表**。唯一的受控例外是多行业事实：`industry_pairs_json` 保存已归一化的 `{l1,l2}` 对，GIN/JSONB 条件支持“任一行业命中”；`industry_l1/l2` 仅保留首对兼容投影，不再是独立事实源。加字段的成本问题由指标注册表解决（§5）。
- **定性补充五栏**（`entity_profile_section`）：`identity / business_product / tech_team / ops_quality / deal_terms`，每组标题均为「其他」，只承接结构化字段装不下的定性内容。原“产业链位置”并入“业务与产品”，原“出售诉求与风险”并入“交易属性与出售诉求”。它保留多修订与当前值；不再人为计算“画像待补”或以三态提示覆盖缺口。
- 前端合并展示为一个「标的信息」页：五组，组内先结构化字段、后「其他」补充栏，参与初筛/打分的字段带「筛」角标。信息页的全部业务事实均可编辑；手动编辑通过统一写入通道，记录操作人、时间、来源和更新记录。
- 买家需求详情的「买家信息」tab 只展示 `buyer_party` 的全局共享资料：买家名称、所属行业、所在地区、联系人、联系方式、其他；每项独立进入编辑态并写回同一主体，因此会在其所有关联需求中同步。主体所属行业/所在地描述买家自身，绝不与收购条件混用。当前需求的收购行业、目标地区和交易条件只属于 `buyer_intent` 的「需求信息」；行业复用一级/二级行业多选字典，主体所在地复用省/市选择器。

### 2.3 数据字典原则

- 行业：`industry_pairs_json` 是唯一的标的行业事实源，保存一个或多个归一化的 `L1/L2` 对；筛选时任一对命中即可。`industry_l1/l2` 是为存量读路径保留的首对兼容投影，必须由该 JSON 派生，不能独立写入。原始行业表述只留在业务更新、证据和解析审计中，不设标的宽表兜底列。
- 所在地：只保留 `location_province / location_city / location_district`。LLM 按证据直接判断，无法确定的层级为空，页面显示 `-`；筛选按用户填写到的层级匹配，不设别名字典、原文列或粒度标记。
- 枚举字段的取值及中文名由指标注册表统一声明；注册表同时声明分组、筛选角色、可写来源和手动编辑权限。

### 2.4 级别模型（标的与买家需求共用）

- **级别是推荐初筛的唯一闸门**：标的 `seller_target.target_grade`、需求 `buyer_intent.intent_grade`，取值 `A/B/C/D/E`。`E` 不进推荐，`A–D` 都进；`A–D` 之间**不影响召回也不影响排序**，只服务筛选与人工优先级。
- **原状态列没有删，降级成「`E` 的细分原因」**：`lifecycle_status`（`sold` 已售出 / `off_market` 已停售）与 `buyer_intent.status`（`paused` 暂停推荐 / `closed` 结束推荐）。前端显示成复合标签 `E（已售出）`，详情页是一个六选一的下拉，不做两步选择。
- **两列由 DB CHECK 严格双向绑定**：`grade ∈ {A,B,C,D} ⟺ 原因列 = active`，`grade = 'E' ⟺ 原因列 ≠ active`。约束是兜底，唯一允许决定这一对取值的地方是 `backend/app/services/entity_grade.py` 的 `resolve_grade_pair`，任何写入方都不许自己拼组合——两列耦合比一列多义更容易漂，而漂了不会报错，只会让标的静默进出推荐池。
- **写入规则**：新建默认 `C`（建表默认值 + 创建接口默认值，创建时不收 `E`）；AI 解析与业务更新只在材料明确给出级别或明确表示停售/停推时才写，**未提取到就不写**；AI 只能进 `E`、不能出 `E`，出 `E` 只能由人显式选一个 `A–D`。
- **两个反模式**：① 解析路径里写 `grade = 提取值 or 'C'` 会让重新解析把顾问设成 `D` 的标的打回 `C`——默认值只在实体创建这一个点生效；② 提示词里出现「未提及则默认 C」会让模型每次解析都主动吐一个 `C`，把人工级别当场覆盖——默认值是数据库的事，不是模型的事。
- **闸门有五处独立实现**（`recommendation_flow` 两处、`services/screening_sql.py`（对话链路的初筛，0817 新增）、`recommendation_agent_tools` 的按 id 取详情、`search_docs` 两处），漏改一处不报错，表现是「E 的还在被推荐」或「新建的搜不到」。改闸门口径时五处必须同改，`tests/test_seller_target_status_model.py` 钉住了这一点。
- **AI 处理进度是与级别正交的另一根轴**，由服务端统一合并解析与调研两条流水线。运行态来自 `information_status=parsing/researching`；终态用 `last_parse_at` 与 `last_research_at` 判断最近一次操作，再结合 `parse_failed` / `research_last_outcome` 显示「解析失败 / 调研失败 / 已完成 / 未处理」。前端只渲染 API 返回的 `ai_processing_state/detail`，不复制判定表。
- 解析与调研在任务入队前原子占用同一个标的；已有任一流水线运行时，另一流水线返回 409，不允许出现“任务已入队、状态未置位”。所有成功和最终失败出口都必须释放运行态并记录对应终态时间。
- `pending_review` 不再是标的状态。未自动采纳的 `extracted_action` 自身仍可保持待复核并留痕，但不得污染 `seller_target.information_status`。
- 2026-07-27 已按两阶段完成 `recommendation_status` 的生产迁移：阶段 A 的 `005 / 20260727_0052` 仅回填、规范化、增加 `last_parse_at` 和并存索引，并在 Railway 全部 API/worker 切换后完成生产验收；阶段 B 再以独立的 `006 / 20260727_0053` 删除旧索引、约束和列。两个阶段禁止合并为一次 preDeploy 迁移。
- 2026-08-14 的级别改造沿用同一节奏：`017 / 20260814_0064` 是阶段 A（加列、回填、加约束、换索引，旧列保留），删旧列另开迁移，等 Railway 全部服务跑上阶段 A 代码之后再做。方案见《标的与需求级别改造施工单0814.md》。

## 3. 关键流程

### 3.1 信息录入与解析（买卖两侧同一机制）

```
顾问从标的/买家意向选择「更新基本信息 / 记录跟进 / 两者都更新」
  → 一份原始文字和附件落 business_update
  ├─ basic_info → 专用 update parser → extracted_action → apply
  │              → 写实体字段/画像 → action_application_log（可回滚）
  └─ follow_up → 用户先选定 buyer_intent × seller_target 关系
                 ├─ 直接写入 → 原文立即成为 relation_event.content
                 └─ AI 整理 → 先建“AI解析中” relation_event 占位
                    → relation_followup_draft_parser 整理 content/next_step
                    → worker 原位回填；最终失败则把同一事件标成可编辑/可删除的解析失败
```

**路由与提交规则（2026-08-03 更新）**：处理范围由顾问在入口显式选择，不让通用 Agent 猜测。`both` 让两个独立任务读取同一来源，任一分支失败不回滚另一分支；OCR 完成后仍按原范围扇出。跟进节点不识别或创建关系、不改推进状态、不选择动态类型；关系和动态类型由顾问选择。顾问可选择直接写入原文，或「提交并 AI 整理」后立即离开：后者在提交事务内先创建时间线占位，不依赖页面轮询或二次确认，worker 成功后自动回填，最终失败后保留原记录供删除或手工编辑。没有已有关系时先走「开始推进 / 关联对手方」。实体级旧跟进不再存在，所有跟进事实统一进入关系时间线。

**字段写入规则**：手动编辑、解析采纳与调研采纳均收敛到**唯一字段写入服务**：`写入(实体, 字段, 新值, 来源, 证据)`——内部查注册表做来源授权、合法性校验与归一化 → 写列 → 记审计和字段来源。原则一句话：**来源可以多，写入通道必须只有一条。**

**买家需求新建与主体复用**：用户提交创建时，系统自动按输入名称对全库 `buyer_party.buyer_name / aliases_json` 做转义后的 `ILIKE` 包含查重；有命中时必须列出全部疑似重复主体并二次确认。历史 `legal_name` 已迁入别名，不再单独维护。用户可跨负责人复用已有买家主体，新需求仍归当前创建人负责；复用后，主体资料会在全部关联需求详情共享。普通买家列表与编辑权限继续遵守 `owner_scope`，不会因查重复用而放宽。

**买家需求附件唯一解析链**（2026-08-03 定稿）：新建买家需求时，`business_update` 只作为附件、OCR 与证据载体；OCR 完成后只投递 `buyer_intent_parse`，由 `buyer_intent_semantic_parser → buyer_intent_normalizer` 两段式专用链解析，严禁同时投递 `business_update_extract_actions`。通用业务更新抽取仍服务业务更新入口，不是买家新建入口的第二写入者。专用解析在最终写入边界按公共条件/同名方案分别裁决：当前值为空则自动补充，归一化后相同则 no-op，仅当前非空且新值不同才进入待确认；类型不合法或作用范围/规则含义不明确的建议不得一键采纳。结构化字段已承载的描述必须从同模块「其他」中去重。

### 3.2 标的调研 Agent（2026-07 已按此重写）

- 架构：**主 agent + 工具**（`web_search` / `fetch_page`），模型自主规划检索词、判断结果、决定收尾；代码不做规则限制（无锚点闸门、无取证计数器、无代码侧冲突分类——否决清单见 0721 方案 §8，不再重开）。
- Agent 输出事实、期间与 `sources`，代码按当前值确定 `relation`（consistent/supplement/temporal_update/same_period_conflict）。文本不做语义相似或日期推断：当前为空是补充，去掉首尾空白后文字完全一致是一致，其余一律是冲突；核心财务字段仍按财务期间区分新期间更新、同期冲突与禁止旧期覆盖。
- 可追溯的补充、一致项和新期间财务信息自动采纳（写画像 + 白名单内结构化字段），冲突留在 `research_proposal.pending_review`。待确认冲突直接显示在标的信息对应字段或对应分组的「其他」栏下，支持「采纳 / 修改并采纳 / 忽略」；修改并采纳在同一事务中应用顾问最终值，同时保留原调研值并标记审计。标的列表「AI 处理」按未处理冲突建议数显示「X项待确认」，全部处理后自动消失。
- 30 天内调研过的标的：批量发起时**显式弹窗二次确认**，不做服务器静默过滤。
- 全链路留痕：一次调研一行 `ai_trace`（完整工具调用链 + 统计元数据）。
- prompt 通过设置页版本管理发布，**不写 prompt seed 迁移**。

### 3.3 智能推荐（Agent 化，2026-08-18 阶段四收口）

**主路径是「直接输入一段需求」，不再要求先建买家需求或标的。** 推荐页是一个对话页：一个输入框 +
一段可直接复制给客户的文本。施工单见 `平台优化方案/智能推荐Agent化改造施工单0804.md`。

```
最近 5 轮已完成的「用户原文 + AI 最终正文」+ 本轮消息
  → recommendation_query_parser v0.3.0
      └─ 结合完整问答判断本轮完整当前需求快照（不是代码机械累计）
  → recommendation_agent_to_target v0.2.1（worker，带受控工具）
      ├─ search_targets：每个条件组先独立完整真实筛，单次真实前 20（≤6 次）
      ├─ 依据 eligible_count / excluded_by_condition 决定是否单向放宽
      ├─ 所有非 count_only 结果按 ID 求并集，≤40 全保留，>40 跨组公平收口
      ├─ deep_evaluate_candidates：全程最多一次；结果回灌同一主 Agent
      ├─ get_target_detail：累计 ≤12；ask_user：全程 ≤1 次，默认不用
      └─ 主 Agent 从 deep-eval ranked 选择 3–6 家重点、最多 5 家备选
  → agent_brief（brief_version=2；代码关联名称、facts、筛选来源与放宽状态）
  → recommendation_answer_writer_to_target v0.2.0（API 侧 SSE，无工具，流式）
  → 带站内标的链接的正文 + 独立追问芯片（芯片不进入正文）
```

**本链路的落库契约**：`agent_understanding` 保存解析器完整快照与 `parser_status`；每次工具执行写
`agent_step`（完整条件、真实召回、marginal、放宽依据）；`agent_deep_eval` 保存候选池、ranked/dropped、
定性判定与来源；`agent_brief` 保存 brief v2、最终名单、`selection_source` 与芯片；`agent_answer` 保存最终
链接正文。四种过程消息及 Writer 答案均带 `duration_ms`，完成后与恢复历史时都从落库值显示；Writer 耗时
口径是从 SSE 开始到末字生成完毕。`ai_trace.metadata_json` 另保存 Prompt 版本、工具预算、候选来源、策略拒绝、
最终归一契约及全部截断/丢弃 notes，会话 debug 必须按 `entity_type=recommendation_session + entity_id` 可查。

**候选与放宽不变式**：不同初筛调用不得在 SQL 中排除前批 ID；重复只在汇总阶段去掉，所以例如 3×20、
30 次重复会形成 30 家唯一池，30 家全部进入深评。required 通常不放宽；只有同组此前真实召回过少且能引用
`based_on_call_index` 与 marginal 证据时，Agent 才能单向放宽。排除行业与重大风险由代码注入，永不放宽。
深评一旦调用即冻结筛选；`deep_eval_status=ok` 时最终 ID 只能来自 ranked，不能来自 dropped 或池外。

**降级与终态语义**：解析 unavailable 写 `parser_status=fallback`，schema 不识别写 `schema_mismatch`，两者都
使用空结构化组 + 原话定性兜底，不能让主 Agent 编条件；深评 unavailable/schema_mismatch 均保留真实 SQL 池，
写 `selection_source=agent_fallback` 或 `screening_fallback`，正文明确“深评未完整返回”，绝不伪装成成功排序。
主 Agent 忘调深评时代码自动补跑一次，并把结果交回同一节点做一次无工具收尾。Writer 未配置、超时或流式失败
用同一 brief v2 规则兜底。`agent_aborted` 永远优先：中止轮不续接、不重试、不写 brief/正文/芯片；取消与 Writer
终态写通过同一 advisory lock 串行化，避免另一页签已停止而旧 SSE 仍落答案。

**生产 Prompt 目标版本**：`recommendation_query_parser v0.3.0`、
`recommendation_deep_eval_to_target v0.3.1`、`recommendation_agent_to_target v0.2.1`、
`recommendation_answer_writer_to_target v0.2.0`。Prompt 只通过设置页/API 版本管理，不写 seed 迁移。

- **初筛与打分必须代码化，禁止全库打包给 LLM。** Agent 只负责编排（理解 / 决定筛几次 / 解读 /
  写话）；Agent 看到的永远是每次筛选返回的候选摘要。
- **对话链路的初筛 0817 起是纯 SQL 硬筛**（`services/screening_sql.py` + `screening_schema.py`，
  施工单《推荐升级阶段一施工单_初筛skill0817.md》）：条件全部翻成 where 子句，**缺失即出局**，
  返回逐条件的 marginal 淘汰拆分（去掉这一条能多召回几家、其中多少只是没录数据），
  命中集按级别 A-D 排完再截前 20。它不打分、不做 OR、不认软硬——多方案由 agent 拆成多次调用。
  工具 schema **从指标注册表生成**（`screening=True` 的买家字段），行业闭集运行时从字典注入，
  所以模型填不出不存在的字段、也填不出字典外的行业名。
  需求单那条链路仍是 `_candidate_targets_for_intent` 的「全表 + 打分」，两条路排序规则不同是设计
  使然（那条有匹配分数，级别不该干扰分数；这条硬筛后幸存者等价，级别就是排序键），代码两处注释互相指认。
  **两个坑写在这里**：① `unknown` 不是 `null`——多个枚举列是 `not null default 'unknown'`，
  判「字段缺失」时两者必须等价；② 负债率与股比两侧存的都是**百分数**，schema 的 description
  必须显式声明单位，写成「0-1 小数」会让条件一家也筛不到且不报错。
- **两个方向是两套流程，不是一个流程的参数**：SQL、工具集、prompt 各自独立，会话的 `mode` 由后端强制
  不可变（`recommendations.py` 拦 mode 不匹配 → 400），前端在发出第一条消息后锁定方向。
  Agent 节点**刻意没有共用兜底节点** —— 两个方向的工具集不同，一份提示词兜不住。
- **本轮只实现正向（为买家找标的）**。反向（为标的找买家）暂时不可用，标的列表的「推荐买家」入口
  已一并移除，推荐页的反向 tab 置灰。反向 Agent 化时真正要解决的是：把一段出售需求描述解析成能驱动
  `_candidate_intents_for_target` 初筛的标的画像 —— 该 SQL 完全依赖标的的结构化字段，字段空则初筛失效。
- **数字来自代码，不来自模型**。候选行带 `facts`（净利/地区/控股/PE/营收/估值，`_target_facts`，枚举
  标签取自指标注册表），brief 组装时用 `id` 关联回代码持有的 facts；模型报了不在候选里的 id 直接丢弃。
- **标的名 → 链接由后端确定性回填**（`recommendation_answer.backfill_target_links`）：数据库全名优先；
  Writer 省略公司法律后缀时，只接受不少于 4 字且在本轮 brief 内唯一的简称；长名优先、每个标的至多一次、
  已链接不重复包。模型永远不输出 id 或 URL；清理模型 URL 时只匹配受限路径字符，不能用 `\S+` 吞掉后续中文。
  前端只渲染站内路径链接，复制按钮输出去掉链接的纯文本。
- **流式只做最后一段**。没有 Redis（队列是 Postgres 表），worker 内的流式推不到浏览器，因此编排在
  worker、写作在 API 的 SSE 端点。`llm_client.stream_openai_compatible_chat` 是独立的无工具函数，
  刻意不碰 tool_calls 的分片累积解析。写作节点未配置或调用失败时回落 `fallback_answer_markdown`
  （用同一份 facts 规则拼装），用户仍拿得到可发的文本。流可恢复：答案落库后重连直接重放，不二次计费。
- **过程与耗时必须可见**。Agent 每次调用工具就写一条 `recommendation_message`(`agent_step`) 并立即提交，
  前端 1.2 秒轮询增量渲染成「已筛选 2 次 · 正在细看 12 家」的折叠行。展开后逐行显示需求解析、每次筛选/
  详情读取、深评、主 Agent 收口和 Writer 的后端 `duration_ms`；收起显示总耗时，亚秒节点显示如 `0.4s` 而非空白。
  Writer 流式期间秒表继续走。Agent 自行放宽条件这类决定必须出现在这里，否则结果的来路不可见。
- **推荐页只干推荐**：不建标的、不建买家需求、不出报告、不做推进维护。「开始推进」移到标的详情页。
  会话仍带 `temporary_filter` 标记，现有守卫继续保证它永远变不成一条撮合关系。
- 澄清默认不问：条件不全时先按现有条件出结果、在结尾提示可补充什么；只有条件空到无法收敛才调
  `ask_user`，且最多一轮、最多 3 题、每题给可点选项并留「跳过」。
- 用户端不展示数字评分、深评档或内部评分明细。分数与评级仍保留在后端用于排序、调试和审计。
  注意与 §2.4 的**级别** A–E 区分：级别是顾问维护、用户可见的推荐闸门，深评档是模型给的内部
  匹配度。两者恰好都用字母，说的不是一回事，文案里不要都叫「A 档」。
- 推荐召回可跨负责人。候选摘要带负责人与「已在推进 / 正与其他买家深入推进」布尔标记，
  后者只给布尔值，不泄露另一关系的对象或阶段。
- **阶段五待拆旧，不在阶段四混删**：旧 `/candidates` 打分链路、旧增量条件操作、旧深评分片/rerank、
  旧推荐消息与兼容分支仍保留；阶段五要先逐一确认无调用方再删除。当前 Agent 主路径正在使用
  `recommendation_query_parser` 与 `recommendation_deep_eval_to_target`，不得再把它们标成“已下线”。
  推荐报告双节点、`recommendation_report` 表与 Word 导出仍保留后端，仅撤掉推荐页入口。
- `worker-llm` 的 stale 窗口从默认 300s 提到 1800s（两套部署已同步）：一轮 Agent 跑满工具预算会超过
  5 分钟，窗口盖不住会把还在干活的任务判死。Agent 侧另有 240 秒墙钟预算强制收尾，双保险。
- **多轮上下文带原文，不做压缩**。最近 5 轮**已完成**的对话，以 `<history_context>` 包裹的
  `<user>：` / `<AI>：` 原文注入（`agent_history_context`，独立 prompt 变量而非塞进 JSON，否则标签会
  被转义成一行、不再是模型看得见的边界）。带原文的理由：用户的跟进问题就是以「他看到的那段正文」和
  「他自己问的那句话」为背景写的，喂给模型的东西一旦与用户看到的不一致就是一次误解的机会。工具结果
  （候选明细）仍然不带 —— 每轮重新筛，旧数字会过期。超预算时**按整轮从最旧的丢，绝不截断**。
- **停止 = 协作式真取消，`agent_aborted` 标记优先**。取消接口立即写标记，worker（`tool_loop` 的
  `should_abort` 钩子，在无请求在途的两个检查点轮询）与 SSE 只管退出；停止延迟 = 当前模型调用 + 当前
  工具调用。三个进程可能同时在收尾一轮刚被停掉的对话，因此规则是**只要有标记这一轮就是中止的**，
  不管别的东西落库没有 —— 中止轮不进上下文、不计入 5 轮、不续接、不重试。刻意不复用
  `early_stop_instruction`：那个会再花一次调用让模型收尾，而用户按停止时不该为不想要的答案付费。
- **正文只在 SSE 的 `done` 事件里落库**，所以中途关页签会留下「有 brief 没正文」的轮次。恢复会话时
  必须自动补连一次 SSE（端点首次连接即生成并落库），否则那一轮永远是个空白气泡。会话列表的运行态也
  必须覆盖此 Writer 间隙：最新一轮有 brief、无 answer/aborted 时，所有页签都显示黄点；答案落库后才转绿。
- **追问芯片是用户下一句要说的话本身**，不是给用户的建议或待确认事项；点击后原样作为下一轮用户消息。
  主 Agent Prompt 用第一人称/祈使/陈述正反例约束，代码再保守丢弃“建议/可补充/可以考虑/明确是否/确认一下/
  需要进一步”等顾问口吻并把决定写入 trace notes。芯片只在正文后显示，从 brief 恢复，不写进正文。
- **附件两条路**：文档走 OCR，正文进输入框供用户核对与编辑；图片按平台策略不进 OCR（节点对图片只返回
  `skipped`），只传 `attachment_id`，由 worker 挂到 Agent 节点自己的多模态消息上。因此
  **Agent 节点必须配多模态模型**。类型与大小一律读 `attachments.uploadPolicy()`，不在前端第二次写死。
- **`get_target_detail` 会把按 id 取到的标的登记进 `candidates_by_id`**（含代码算出的 facts 与
  「别的买家在深入推进」警示）。带回历史原文后，跟进问题会拿上一轮正文里的 id 直接取详情；不登记的话
  最终那道 join 会把它当成模型编的 id 丢掉，用户问「第二个再详细点」得到的是一片空白。
- **Agent 会话在「最近推荐」里按开场问题命名**（`initial_condition_snapshot_json.agent_session` 为真时
  走这条分支），副行是「N 轮对话 · 相对时间」；开场那句话同时写进 `anonymous_input_snapshot`，会话搜索
  匹配的就是这一列。旧的临时筛选会话保持原来的「临时条件筛选」标签。
- **回到已有买家需求只是一个弱入口**：空态输入框下的一行文字链，选中后把需求拼成**结构化摘要**填进
  输入框，仍然不锚定 session、不创建任何对象。注意 `industry_secondary` 实际上常常在一列里塞了多个用
  「、」连起来的行业，按数组去重去不掉，必须先按分隔符拆分。
- **提示词模板变量只认 `{{ name }}`**（`backend/app/ai/prompting.py`）。写成单花括号不会报错，
  模板存得下、渲得过、模型也照常调用工具，只是模型收到的是字面量 `{answer_brief_json}` ——
  它会回一句「您似乎忘记替换变量了」，而系统里没有任何一处指出原因。存提示词时已加校验：
  单花括号包住已声明变量、或一个已声明变量都没用到，都会被拒绝（新建与编辑两条路径都过）。
- **新增节点需要在设置页建配置和 Prompt 才能工作**（不写 prompt seed 迁移）：
  `recommendation_agent_to_target`（必配，否则推荐直接失败；prompt 变量是
  `recommendation_context_json` + `history_context`）、
  `recommendation_answer_writer_to_target`（不配则走规则兜底文案）。

### 3.4 撮合进度管理（已实现）

**核心决策：进度的单元是「买家意向 × 标的」这一对（`buyer_seller_relation`），不是任何单个实体。** 每对唯一、九状态流水线、事件挂关系（`relation_event`）。"多线程且交叉"由此消解：交叉只是同一条线从两个详情页可见。

四个视图：
1. **标的详情「推进」tab**：该标的的全部关系（买家、状态、最新动态、下一步、无动态预警）；
2. **买家意向详情「推进」tab**：完全镜像；
3. **关系时间线**（点开一对）：事件流 + 状态变更 + 下一步 + 记录动态；
4. **全局看板**：只做概览 + 跳转。九状态收敛为「初步接触 / 深度交流 / 已成交」三主列和「已关闭」底部抽屉；支持按标的/按意向双视角、责任范围三态、搜索与“隐藏最近无动态”筛选，固定按最近关系动态由近到远。同列同主体的多条关系默认折叠成摞，但每张内层卡仍是一条独立关系。点击卡片通过 `?tab=progress&relation=<id>` 深链接到两侧详情并展开对应时间线。

标的管理与买家管理列表只展示实体自身的「更新时间」（`updated_at`，前端格式 `MM-DD HH:mm`），不在实体列表聚合或展示关系的最近跟进/推进；关系动态只在上述四个进度视图中读取。

看板取数使用 `/relations/board` 瘦端点（9 个固定字段），前端一次加载后完成分桶、折叠、筛选和计数；达到客户端上限会显式提示截断，不再静默丢数据。默认责任范围为「我参与的」，另有「全部 / 我全权的」；管理员可再按指定负责人筛选。指定负责人与责任范围是 AND，且使用独立 SQL 参数，不能覆盖或放宽负责人权限地板；顾问构造他人 owner 参数也只能看到自身权限与该筛选的交集。

**动态来源与路由**：当前关系由推荐「开始推进」或手动「关联对手方」两条确定性路径创建。手工动态（含 AI 自动回填的跟进）允许修改和软删除；系统开局和状态变更事件不可修改。录入跟进时必须先选一条已有关系；直接写入保留原文，AI 路径只整理沟通内容与下一步并自动回填同一占位事件。同一份材料需要同时更新事实时，由用户选择「基本信息和跟进都更新」，两个固定节点独立处理。系统不通过自由文本自动识别对手方或创建关系。

**三个业务规则（2026-08-03 更新）**：
1. **深入推进提示**：`due_diligence / agreement` 中的关系不排除另一侧继续参与推荐；标的侧只提示「正与其他买家深入推进」，买家需求侧只提示「正与其他标的深入推进」，**不透露对方是谁**。早期接触、已成交与关闭状态均不算“正在深入推进”。
2. **跨负责人可见性**：一条关系对双方负责人 + 管理员可见。
3. **状态集**：沿用 schema 九状态跑真实业务，之后按实际使用裁剪。

### 3.5 附件与 OCR

`attachment → OCR（外部 API）→ parsed_document → 按 business_update.processing_scope 进入 3.1 对应分支`，`evidence_span` 记录字段值在原文中的出处。OCR 是外部 HTTP 服务，不绑 AI 节点、不吃 Prompt；配置以 `provider_type='ocr'` 存入 `model_provider_config`，设置页支持新增、编辑、启停与直接录入 API Key。直接录入的 Key 经 Fernet 加密且永不回显，API 与 worker-ocr 读取同一条数据库配置；仅在没有启用的数据库配置时回落到环境变量。文本 PDF 与 Office 文件本地提取，扫描 PDF 才调用 Doc2X，图片直接进入多模态模型。

### 3.6 数据看板

数据看板定位为**库存画像**，对顾问与管理员均可见，不承载账号活跃度考核，也不展示标的或买家名称。2026-07-29 拆除原按账号活跃度表并完成正式实现，四块内容三行版面：**标的省级分布地图 / 买家省级分布地图 / （标的行业分布 top + 标的营收规模）**。页面不写标题也不写口径说明，右上角只有服务端取数时间。

- **取数**：`/stats/overview`，**全库、不分级别、不按 `owner_scope` 过滤**——回答的是「盘子长什么样」而不是「我手上有什么」，所以大屏上每个人看到的是同一张图。响应只含计数，不含任何名称。
- **底图**：DataV.GeoAtlas 省级边界，含台湾省与南海诸岛（九段线画在右下角插图框）。`scripts/build_china_map.py` 离线做 Albers 等积圆锥投影 + Douglas-Peucker 简化，产出 `frontend/src/features/dashboard/chinaMap.ts` 的 34 条 SVG 路径；**运行时不解析 GeoJSON，也不引入图表库**——前端运行时依赖仍是 5 个。底图 chunk 约 136 KB，按需加载不进主包，两张地图共用同一份。
- **着色**：固定阈值分档，不做数量编码——同一个颜色在两次打开之间含义必须不变。**分档表按指标各配一份**：标的用 1–2/3–5/6–10/11+（红），买家主体量级只有一半、单省最多 2 家，用 1/2–3/4–6/7+（金）；共用一张表会让买家图整张落在最浅档。量级整体上台阶后回来调 `distribution.ts`。
- **买家地图画的是买家主体所在地**（`buyer_party.region_province`），不是买家想买哪里。需求地域 `buyer_intent.region_scope_summary` 是自由文本（「优先考虑长三角、珠三角区域具备扩产潜力的项目」），要上地图得先有结构化地域字段，属于买家侧注册表接入的后置项。
- **行业按二级行业取 top 10**，一级行业太粗读不出赛道。两个已知失真必须在页面上说出来：一个标的可挂多个 L2 所以计数之和大于标的数；二级行业字典的同义词尚未合并（食品 / 食品制造 / 食品加工 各占一行），榜单因此偏低估——去重工单见 `平台优化方案/数据看板拆除验收单0729.md` §1.3，是二级赛道图不失真的前提。

## 4. 技术决策清单（现行且已定，不再争论）

| 决策 | 状态 |
|---|---|
| 初筛/打分代码化，宽表承载，多方案 OR + 三态 | 已实现（0720 方案） |
| 深评分片并发 + ID 回写协议 | 已实现 |
| 调研 agent = 主 agent + 工具，LLM 自主规划 | 已实现（0721 方案） |
| LLM 客户端支持 tool calling + 通用 tool loop | 已实现（`ai/tool_loop.py`） |
| 每节点模型/prompt 可配置、版本管理即时生效、prompt 不走迁移 | 已实现 |
| `ai_trace` 全链路留痕，调试中心可查 | 已实现 |
| 唯一字段写入通道 | R4 实施中：解析采纳、调研采纳和信息页手动编辑均须走注册表校验的 writer；负责人、生命周期等非信息页操作仍走专用流程 |
| 指标注册表为单一事实源 | R4 实施中：seller_target/buyer_intent 的可写字段、枚举、前端元数据与解析/调研消费方均从注册表派生 |
| 服务层原则：同一业务动作，API 调和后台任务调必须是同一个函数 | 逐步下沉中 |

## 5. 指标注册表（设计）

**问题**：一个指标今天散在五处——DB 列、`recommendation_flow` 读取、前端 `infoGroups.ts`、extracted_action apply 白名单、调研字段白名单——靠人肉同步 + 缝合测试（`test_action_type_sync` / `test_info_group_sync`）兜底。

**形态**：`backend/app/registry/indicators.py`，纯声明式：

```python
@dataclass(frozen=True)
class Indicator:
    entity: str                # "seller_target" | "buyer_intent"
    column: str                # DB 列名 = API 字段名
    label: str                 # 中文名（前端/更新记录/prompt 上下文共用）
    group: str                 # 五组之一（identity + 四个业务分组）
    kind: str                  # text | yuan | ratio | enum | date | bool3
    enum_options: tuple | None # 枚举 code 与中文名
    screening: str | None      # None | "hard" | "soft:<权重>"
    writable_by: frozenset     # {"manual", "parse", "research"} 子集
    deep_eval: bool            # 是否进入深评上下文
```

**五个消费方的接入方式**：
1. 前端：`/meta/indicators` 驱动分组、中文名、枚举、编辑权限与「筛」角标——**运行时驱动，不做代码生成**，`infoGroups.ts` 仅保留值格式化；
2. 打分器：一致性测试改为「`recommendation_flow` 读取的列 ⊆ 注册表 screening 列」，替代正则扒源码；
3. extracted_action apply 白名单 = `writable_by ∋ "parse"`；
4. 调研 `allowed_structured_fields` = `writable_by ∋ "research"`；
5. 字段写入服务用 `kind/enum_options` 做校验与归一化，并按 `writable_by` 拒绝无权来源；更新记录用 `label` 显示。

**迁移路径**：立注册表（先只覆盖 seller_target）→ `/meta/indicators` 上线 → 前端切换 → 两个白名单切换 → 缝合测试改写或删除 → buyer_intent 接入。每步可独立发版。

## 6. 实现判决（2026-07-22 审计，方法：写入路径/读取路径/种子/前端展示四查）

### 6.1 表级判决

**判死（baseline 不再携带）**：

| 表 | 证据 | 判决理由 |
|---|---|---|
| `seller_target_financial` | 零读零写零种子 | 多期财务从未接线；current_* 列已承担最新期。需要多期时重新设计 |
| `buyer_intent_constraint` | 零读零写 | 已被 `buyer_intent_scenario`（多方案条件模型，043）取代 |
| `seller_target_risk` | 只读不写，读永远为空 | `risk_summary` 列已承担风险信息；删表并清理 2 处空读 |
| `seller_target_tag` + `tag_dictionary` | tag 表零写；字典只有种子+1 处 meta 读 | 标签体系从未接线；行业/地区已有专表字典 |
| `seller_party` | 零写；仅调研上下文 LEFT JOIN，永远 JOIN 出 NULL | 吸收：`unified_credit_code` 等列如未来需要，加到 `seller_target`；删表并清理 research.py 的 JOIN |
| `region_alias_config` | 种子 + 仅 meta 健康检查数行数，零业务逻辑零前端 | 省/市由 LLM 解析直接提取，不需要别名字典；删表并清理 meta 健康检查 |
| `target_follow_up` / `buyer_intent_follow_up` | 跟进不能脱离买家意向 × 标的关系表达；旧记录无法无损映射 | 2026-07-28 退役，历史数据按产品决策删除；`relation_event` 成为唯一跟进事实源 |

**冻结（保留但声明不启用）**：`team` / `workspace` 表及各表 `team_id`/`workspace_id` 列——单租户常量，删除的代码扰动大于收益；多租户需求出现时再激活。**`visibility` 列除 attachment 外全部删除**（存值但无过滤行为，误导语义；attachment 的入参校验一并简化）。

**其余业务表：活，保留。**（`evidence_span`、`field_value_source` 经查为活表——证据链与字段来源展示在用，此前"疑似死"的判断错误。）

### 6.2 列级判决（宽表静默列，baseline 一并清除）

`seller_target` 判死 14 列：
- 零引用 10 列：`acceptable_relocation_regions_json`、`accepted_payment_methods_json`、`asset_regions_json`、`control_path_options_json`、`deal_paths_json`、`operating_regions_json`、`production_regions_json`、`pe_calculation_basis_json`、`completeness_score`、`last_business_update_at`
- 残迹引用、无真实读者 4 列（2026-07-22 复核确认）：`listing_board`（审计命中实为 buyer_intent 别名键误配，真实零引用）、`registered_country`（仅解析上下文 SELECT，恒空）、`seller_party_id`（随 seller_party 判死）、`last_attachment_parse_at`（只写不读的遥测残迹）

`buyer_intent` 判死 3 列：`is_temporary`、`last_business_update_at`、`last_recommendation_at`（零引用）。

### 6.3 迁移压平（2026-07-22 完成）

47 个 SQL 迁移 + 48 个 alembic 版本脚本已压成 `database/migrations/001_baseline.sql` + 单一 alembic 修订（沿用压平前的修订号 `20260722_0048`，生产部署零感知 no-op，无需 stamp）。要点：

- baseline 从生产 pg_catalog 快照生成（35 表 / 690 列 / 637 约束 / 126 索引），种子含默认团队与用户、197 行行业字典（含别名行无损重建）、9 个现行默认 prompt 版本——**prompt 语料自此进入版本控制**，兼作备份；
- CI 常驻验收 job `Fresh database from baseline`：pgvector/pg17 空库应用 baseline → 与 `tests/fixtures/schema_snapshot_production.json` 逐项比对（NOT NULL 目录条目两侧排除，对 PG 大版本免疫）→ 再 `upgrade head`。首跑即抓出两个「生产已就位、空库才暴露」的隐性依赖：行业字典自引用外键的插入顺序、pgvector/pg_trgm 扩展前导；
- 新迁移从 `002_` 起编号，splitter 与 `test_migration_sql.py` 照旧服务后续迁移；
- `/debug/table-stats` 保留为运维端点，`/debug/schema-snapshot` 已完成使命并移除。

### 6.4 服务层下沉

判断标准：**同一个业务动作，从 API 调和从后台任务调，必须是同一个函数。** 已下沉：`research_apply`、`profile_sections`、`business_update_flow`、`recommendation_flow`、`extracted_action_apply`。待下沉：seller_targets / buyer_intents 路由内的写库+审计逻辑（随字段写入服务一并完成）。

## 7. 未决问题

| 问题 | 状态 |
|---|---|
| 进度管理 UI（四视图） | ✅ 2026-07-22 完成（施工单 R2a–R2d），真机验证 |
| 跟进解析如何确定对手方 | ✅ 2026-07-28 定稿：不做 AI 实体路由；用户先选已有关系，AI 只整理草稿 |
| 一次录入如何同时产出事实更新和关系事件 | ✅ 2026-08-03 更新：用户显式选 scope；同一来源扇出 basic_info / follow_up 两个独立任务；跟进可直接写入，或先占位再由 AI 自动回填，不再二次确认 |
| 需求驱动调研触发（推荐前自动补缺口，0720 §12.1） | 移交本总纲，待排期 |
| 买家侧画像与调研 | 用户明示暂缓 |
| 数值类结构化字段进调研白名单 | 暂缓（当前白名单仅文本/枚举 11 字段） |
| OCR key 并入加密配置体系 | ✅ 2026-08-02 完成：设置页可新增/编辑/启停 OCR 配置并直接录入加密 Key；环境变量仅作兜底 |
| 前端验证流程（本地 dev server 连生产 + 浏览器验证） | 环境已通，待常态化 |
| 标的侧四个新列暂无买家对手方 | ⚠️ **计划内的临时不对称**。`major_risk_flags_json`、`acceptable_transaction_structures_json`、`main_products_text`、`stock_code` 于 2026-08-06 随迁移 `015` 落地，买家侧对手方与打分接线排在下一轮（见《标的指标体系重构施工单0806.md》§三、§六）。**不要当成缺陷去补，也不要判死**：本轮只建事实列，先攒数据 |
| 买家侧静默失效字段与问卷缺口 | 待下一轮。`max_premium_rate`（问卷第 3 项）、`transaction_types_json`（第 7 项）两侧有列但打分器零读取；`earnout_requirement` 的枚举语义本身是坏的；`budget_min/max_yuan` 无问卷入口。判决见《标的指标体系重构结论0805.md》§6.8 / §7 |
| 上市与非上市买家的整体价值轴权重不一致 | 待下一轮（属打分器）。问卷第 2 项是同一个问项，买家填「市值范围」进 6 分维度、填「估值」进 14 分维度，两组字段都填时还会重复计分。需在打分前按 `acceptable_listed_status_json` 归一成一条 C-a 轴 |

## 8. 附录：审计口径

- 表级：正则扫 `backend/**/*.py` 的 `insert into/update/delete from`（写）与 `from/join`（读），词边界防前缀误配；迁移单扫 `insert into`（种子）。
- 列级：001 建表块 + 全部 `alter table add column` − `drop column`，对后端 `*.py` 与前端 `src/**/*.ts(x)` 做词边界计数。泛用列名（status 等）计数含噪声，仅作参考。
- 局限:审计的是**当前代码**；历史版本可能写过后来失去写入路径的表（如 seller_party），故 baseline 前仍需生产行数盘点（§6.3 第 1 步）。
